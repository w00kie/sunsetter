[project]
name = "sunsetter"
version = "2.0.0"
description = "Find the days when the sun rises or sets along a chosen sightline"
requires-python = ">=3.13,<3.15"
dependencies = []

[dependency-groups]
dev = [
  "workers-py>=1.7.0",
  "workers-runtime-sdk>=0.3.0",
]

[tool.uv]
package = false
